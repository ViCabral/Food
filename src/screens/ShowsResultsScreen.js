import React from 'react';
import {View, Text, Image ,StyleSheet} from 'react-native';

const ShowResultsScreen = ({ result }) => {
    
    return (
        <View >
            <Text> Works</Text>
        </View>
    )
};

const styles = StyleSheet.create({
});

export default ShowResultsScreen;